# Özel evrak: varsayılan /private-documents-proxy.php (Hostinger kökü + data/private-documents-proxy.config.php).
# Doğrudan porta POST (CORS gerekir):
# PUBLIC_PORTAL_PRIVATE_DOCUMENTS_URL=https://portal.campusgerman.com/api/webhooks/private-documents
# PUBLIC_WEBHOOK_SECRET=...

# Stripe Embedded Checkout publishable key sunucuda tutulur:
# public/data/booking-pay-proxy.config.php → stripe_publishable_key = 'pk_live_...'
