{
  "name": "campusgerman-astro",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "build:hostinger": "astro build && node -e \"try{require('fs').rmSync('dist/k',{recursive:true,force:true});console.log('dist/k kaldırıldı (Hostinger için k.php kullanılacak)');}catch(e){}\"",
    "postbuild": "node scripts/verify-sitemap.js",
    "sitemap:copy": "node scripts/copy-sitemap-for-vercel.js",
    "preview": "astro preview",
    "astro": "astro",
    "postinstall": "patch-package"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.0",
    "astro": "^4.0.0",
    "gsap": "^3.14.2",
    "motion": "^11.18.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.3.0",
    "patch-package": "^8.0.0",
    "typescript": "^5.3.0"
  }
}
