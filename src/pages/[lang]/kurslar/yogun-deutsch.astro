---
export async function getStaticPaths() {
    return [
        { params: { lang: 'tr' } },
    ];
}

const { lang } = Astro.params;

// Türkçe'de yogun-deutsch slug'ını intensiv-deutsch sayfasına yönlendir
return Astro.redirect(`/${lang}/kurslar/intensiv-deutsch`, 301);
---
