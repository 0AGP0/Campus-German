---
export async function getStaticPaths() {
    return [
        { params: { lang: 'es' } },
    ];
}

const { lang } = Astro.params;

// İspanyolca'da aleman-intensivo slug'ını intensiv-deutsch sayfasına yönlendir
return Astro.redirect(`/${lang}/cursos/intensiv-deutsch`, 301);
---
