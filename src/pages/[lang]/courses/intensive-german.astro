---
export async function getStaticPaths() {
    return [
        { params: { lang: 'en' } },
    ];
}

const { lang } = Astro.params;

// İngilizce'de intensive-german slug'ını intensiv-deutsch sayfasına yönlendir
return Astro.redirect(`/${lang}/courses/intensiv-deutsch`, 301);
---
