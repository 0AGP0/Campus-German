---
import '../styles/pages/quote-wizard.css';

interface Props {
  lang: string;
  morningTime: string;
  afternoonTime: string;
  eveningTime?: string;
  startDateOptions: { value: string; label: string; iso?: string }[];
}

const { lang, morningTime, afternoonTime, eveningTime = '19:00 – 22:00', startDateOptions } = Astro.props;

const copy = {
  tr: {
    stepTitle: 'Ders zamanı',
    stepDesc: 'Tercih ettiğiniz ders saati ve başlangıç tarihini seçin.',
    step3Title: 'Ders zamanını seç',
    step3Subtitle: 'İsterseniz ders saati seçin; boş bırakarak da devam edebilirsiniz. Tüm gruplarda haftada 20 saat yüz yüze eğitim yer alır.',
    cardSelectHint: 'İsteğe bağlı: sabah veya öğleden sonra grubundan birini seçin.',
    slotMorningTitle: 'Sabah grubu',
    slotAfternoonTitle: 'Öğleden sonra grubu',
    slotEveningTitle: 'Akşam grubu',
    step3SubtitleOnline: 'Online programda akşam grubu (19:00 – 22:00) sabittir; seçmeden de devam edebilirsiniz.',
    step4Title: 'Başlangıç tarihini seç',
    step4Subtitle: 'Size en uygun başlangıç tarihini seçin veya takvimden bir Pazartesi seçin.',
    step4PresetTitle: 'Kurs başlangıç dönemleri',
    step4CustomTitle: 'Başlama tarihini kendim seçmek istiyorum',
    step4CustomF1: 'Özel başlangıç tarihi (yalnızca Pazartesi)',
    step4CustomF2: 'Esnek planlama',
    step4CustomF3: 'Size uygun program için danışman desteği',
    step4CustomCta: 'Tarihimi kendim seçmek istiyorum',
    step4Trust: 'Güvenli seçim; başarıya giden en doğru adım.',
    step4PickDateLabel: 'Başlangıç tarihiniz',
    step4PickDateHint: 'Takvimde yalnızca Pazartesi günleri seçilebilir.',
    step4CalendarTrigger: 'Takvimden bir Pazartesi seçin',
    step4CalPrevMonth: 'Önceki ay',
    step4CalNextMonth: 'Sonraki ay',
    back: 'Geri',
    continueBtn: 'Devam Et',
    needTime: 'Lütfen bir ders saati seçin.',
    needDate: 'Listeden bir dönem seçin veya özel tarih için takvimden bir Pazartesi seçin.',
    needCustomDate: 'Özel başlangıç için lütfen takvimden bir Pazartesi seçin.',
    needMonday: 'Özel başlangıç tarihi yalnızca Pazartesi olabilir.',
    weekdaysShort: 'Pzt, Sal, Çar, Per',
    hoursPerWeek: '20 saat / hafta',
    hoursPerWeekOnline: '16 saat + 4 etüt / hafta',
    weekdaysShortOnline: 'Pzt – Per',
  },
  de: {
    stepTitle: 'Unterrichtszeit',
    stepDesc: 'Wählen Sie Ihre bevorzugte Unterrichtszeit und Ihr Startdatum.',
    step3Title: 'Unterrichtszeit wählen',
    step3Subtitle: 'Wählen Sie optional ein Zeitfenster — Sie können auch ohne Auswahl fortfahren. Alle Gruppen umfassen 20 Std./Woche Präsenzunterricht.',
    cardSelectHint: 'Optional: Morgen- oder Nachmittagsgruppe wählen.',
    slotMorningTitle: 'Morgengruppe',
    slotAfternoonTitle: 'Nachmittagsgruppe',
    slotEveningTitle: 'Abendgruppe',
    step3SubtitleOnline: 'Beim Online-Programm ist die Abendgruppe (19:00 – 22:00) fest — Fortfahren ohne Auswahl ist möglich.',
    step4Title: 'Starttermin wählen',
    step4Subtitle: 'Wählen Sie Ihren Wunschstarttermin oder einen Montag im Kalender.',
    step4PresetTitle: 'Starttermine zum Kursbeginn',
    step4CustomTitle: 'Ich möchte meinen Starttermin selbst festlegen',
    step4CustomF1: 'Individueller Starttermin (nur Montage)',
    step4CustomF2: 'Flexible Planung',
    step4CustomF3: 'Beratung für das passende Programm',
    step4CustomCta: 'Starttermin selbst wählen',
    step4Trust: 'Sichere Auswahl – der richtige Schritt zum Erfolg.',
    step4PickDateLabel: 'Ihr Wunsch-Startdatum',
    step4PickDateHint: 'Im Kalender sind nur Montage wählbar.',
    step4CalendarTrigger: 'Wählen Sie einen Montag im Kalender',
    step4CalPrevMonth: 'Voriger Monat',
    step4CalNextMonth: 'Nächster Monat',
    back: 'Zurück',
    continueBtn: 'Weiter',
    needTime: 'Bitte wählen Sie eine Unterrichtszeit.',
    needDate: 'Bitte wählen Sie einen Termin aus der Liste oder einen Montag im Kalender.',
    needCustomDate: 'Bitte wählen Sie unten einen Montag für Ihren individuellen Start.',
    needMonday: 'Der individuelle Starttermin muss ein Montag sein.',
    weekdaysShort: 'Mo, Di, Mi, Do',
    hoursPerWeek: '20 Std./Woche',
    hoursPerWeekOnline: '16 Std. + 4 Tutorien / Woche',
    weekdaysShortOnline: 'Mo – Do',
  },
  en: {
    stepTitle: 'Lesson time',
    stepDesc: 'Choose your preferred lesson time and start date.',
    step3Title: 'Choose lesson time',
    step3Subtitle: 'Optionally pick a time slot — you can continue without selecting. All groups include 20 hours of in-person lessons per week.',
    cardSelectHint: 'Optional: select a morning or afternoon group.',
    slotMorningTitle: 'Morning group',
    slotAfternoonTitle: 'Afternoon group',
    slotEveningTitle: 'Evening group',
    step3SubtitleOnline: 'For the online program, the evening group (19:00 – 22:00) is fixed — you can also continue without selecting.',
    step4Title: 'Choose your start date',
    step4Subtitle: 'Select a start date that works for you, or pick a Monday on the calendar.',
    step4PresetTitle: 'Course start dates',
    step4CustomTitle: 'I want to choose my own start date',
    step4CustomF1: 'Custom start date (Mondays only)',
    step4CustomF2: 'Flexible planning',
    step4CustomF3: 'Advisor support for the right program',
    step4CustomCta: 'Choose my own date',
    step4Trust: 'A secure choice — the right step toward success.',
    step4PickDateLabel: 'Your preferred start date',
    step4PickDateHint: 'Only Mondays can be selected on the calendar.',
    step4CalendarTrigger: 'Pick a Monday on the calendar',
    step4CalPrevMonth: 'Previous month',
    step4CalNextMonth: 'Next month',
    back: 'Back',
    continueBtn: 'Continue',
    needTime: 'Please select a lesson time.',
    needDate: 'Choose a date from the list or pick a Monday on the calendar.',
    needCustomDate: 'Please pick a Monday on the calendar below.',
    needMonday: 'Custom start dates must be a Monday.',
    weekdaysShort: 'Mon, Tue, Wed, Thu',
    hoursPerWeek: '20 hrs/week',
    hoursPerWeekOnline: '16 hrs + 4 tutorials / week',
    weekdaysShortOnline: 'Mon – Thu',
  },
  es: {
    stepTitle: 'Horario de clase',
    stepDesc: 'Elige tu horario preferido y la fecha de inicio.',
    step3Title: 'Elige el horario de clase',
    step3Subtitle: 'Puedes elegir un horario o continuar sin seleccionar. Todos los grupos incluyen 20 horas presenciales por semana.',
    cardSelectHint: 'Opcional: elige un grupo de mañana o de tarde.',
    slotMorningTitle: 'Grupo de mañana',
    slotAfternoonTitle: 'Grupo de tarde',
    slotEveningTitle: 'Grupo de tarde-noche',
    step3SubtitleOnline: 'En el programa online, el grupo de tarde-noche (19:00 – 22:00) es fijo; también puedes continuar sin seleccionar.',
    step4Title: 'Elige tu fecha de inicio',
    step4Subtitle: 'Selecciona una fecha de inicio o elige un lunes en el calendario.',
    step4PresetTitle: 'Fechas de inicio del curso',
    step4CustomTitle: 'Quiero elegir mi propia fecha de inicio',
    step4CustomF1: 'Fecha de inicio personalizada (solo lunes)',
    step4CustomF2: 'Planificación flexible',
    step4CustomF3: 'Apoyo del asesor para el mejor programa',
    step4CustomCta: 'Quiero elegir mi fecha',
    step4Trust: 'Una elección segura — el paso correcto hacia el éxito.',
    step4PickDateLabel: 'Tu fecha de inicio deseada',
    step4PickDateHint: 'En el calendario solo se pueden elegir lunes.',
    step4CalendarTrigger: 'Elige un lunes en el calendario',
    step4CalPrevMonth: 'Mes anterior',
    step4CalNextMonth: 'Mes siguiente',
    back: 'Atrás',
    continueBtn: 'Continuar',
    needTime: 'Selecciona un horario de clase.',
    needDate: 'Elige una fecha de la lista o un lunes en el calendario.',
    needCustomDate: 'Por favor elige un lunes abajo.',
    needMonday: 'La fecha personalizada debe ser un lunes.',
    weekdaysShort: 'Lun, Mar, Mié, Jue',
    hoursPerWeek: '20 h/semana',
    hoursPerWeekOnline: '16 h + 4 tutorías / semana',
    weekdaysShortOnline: 'Lun – Jue',
  },
};

const t = copy[lang as keyof typeof copy] ?? copy.tr;

const presetIsos = startDateOptions.map((opt) => {
  if (opt.iso) {
    return { iso: opt.iso, value: opt.value, label: opt.label };
  }
  const parts = opt.value.split('.').map(Number);
  const d = parts[0];
  const m = parts[1];
  const y = parts[2] || 2026;
  const iso = d && m ? `${y}-${String(m).padStart(2, '0')}-${String(d).padStart(2, '0')}` : '';
  return { iso, value: opt.value, label: opt.label };
}).filter((p) => p.iso);
---

<div class="form-step booking-lesson-time-step" data-step="2">
  <div class="booking-qx-wrap">
    <section class="qx-stage-shell qx-stage-shell--step3">
      <header class="qx-scene-head qx-scene-head--step3">
        <span class="qx-scene-kicker">1</span>
        <div class="qx-scene-head__copy">
          <h3 class="qx-question">{t.step3Title}</h3>
          <p class="qx-question-note" id="booking-lesson-time-subtitle">{t.step3Subtitle}</p>
        </div>
      </header>
      <input type="hidden" name="timezone-slot" id="booking-timezone-slot" value="" />
      <div class="qx-time-slots" id="booking-time-slots" role="group" aria-label={t.step3Title}>
        <button type="button" class="qx-time-slot qx-time-slot--morning" data-period="morning" data-timezone-value="morning" aria-pressed="false">
          <div class="qx-time-slot__period" aria-hidden="true">
            <span class="qx-time-slot__icon"><i class="fas fa-cloud-sun"></i></span>
            <div class="qx-time-slot__period-copy">
              <span class="qx-time-slot__period-label">{t.slotMorningTitle}</span>
              <span class="qx-time-slot__period-clock">{morningTime}</span>
            </div>
          </div>
          <div class="qx-time-slot__main">
            <div class="qx-time-slot__foot">
              <span class="qx-time-slot__meta"><strong>{t.hoursPerWeek}</strong> · {t.weekdaysShort}</span>
            </div>
          </div>
        </button>
        <button type="button" class="qx-time-slot qx-time-slot--afternoon" data-period="afternoon" data-timezone-value="afternoon" aria-pressed="false">
          <div class="qx-time-slot__period" aria-hidden="true">
            <span class="qx-time-slot__icon"><i class="fas fa-sun"></i></span>
            <div class="qx-time-slot__period-copy">
              <span class="qx-time-slot__period-label">{t.slotAfternoonTitle}</span>
              <span class="qx-time-slot__period-clock">{afternoonTime}</span>
            </div>
          </div>
          <div class="qx-time-slot__main">
            <div class="qx-time-slot__foot">
              <span class="qx-time-slot__meta"><strong>{t.hoursPerWeek}</strong> · {t.weekdaysShort}</span>
            </div>
          </div>
        </button>
        <button type="button" class="qx-time-slot qx-time-slot--evening" data-period="evening" data-timezone-value="evening" aria-pressed="false">
          <div class="qx-time-slot__period" aria-hidden="true">
            <span class="qx-time-slot__icon"><i class="fas fa-moon"></i></span>
            <div class="qx-time-slot__period-copy">
              <span class="qx-time-slot__period-label">{t.slotEveningTitle}</span>
              <span class="qx-time-slot__period-clock">{eveningTime}</span>
            </div>
          </div>
          <div class="qx-time-slot__main">
            <div class="qx-time-slot__foot">
              <span class="qx-time-slot__meta"><strong>{t.hoursPerWeekOnline}</strong> · {t.weekdaysShortOnline}</span>
            </div>
          </div>
        </button>
      </div>
      <input type="checkbox" name="timezone" value="morning" id="booking-tz-morning" class="booking-tz-hidden" hidden />
      <input type="checkbox" name="timezone" value="afternoon" id="booking-tz-afternoon" class="booking-tz-hidden" hidden />
      <input type="checkbox" name="timezone" value="evening" id="booking-tz-evening" class="booking-tz-hidden" hidden />
    </section>

    <section class="qx-stage-shell qx-stage-shell--step4">
      <header class="qx-scene-head qx-scene-head--step4">
        <span class="qx-scene-kicker">2</span>
        <div class="qx-scene-head__copy">
          <h3 class="qx-question">{t.step4Title}</h3>
          <p class="qx-question-note">{t.step4Subtitle}</p>
        </div>
      </header>
      <input type="hidden" id="start-date" name="start-date" value="" />
      <input type="hidden" id="booking-start-date-mode" name="start-date-mode" value="" />
      <div class="qx-step4-grid">
        <article class="qx-step4-card qx-step4-card--presets">
          <div class="qx-step4-card__head">
            <span class="qx-step4-card__icon qx-step4-card__icon--cal" aria-hidden="true"><i class="far fa-calendar-alt"></i></span>
            <h4 class="qx-step4-card__title">{t.step4PresetTitle}</h4>
          </div>
          <div class="qx-step4-presets" id="booking-step4-presets" role="radiogroup" aria-label={t.step4PresetTitle}>
            {presetIsos.map((p) => (
              <label class="qx-step4-preset-row">
                <input type="radio" name="booking-start-preset" value={p.iso} data-display-value={p.value} data-display-label={p.label} class="qx-step4-preset-radio" />
                <span class="qx-step4-preset-visual" aria-hidden="true"><i class="far fa-calendar-alt"></i></span>
                <span class="qx-step4-preset-text">
                  <strong class="qx-step4-preset-long">{p.label}</strong>
                </span>
              </label>
            ))}
          </div>
        </article>
        <article class="qx-step4-card qx-step4-card--custom" data-booking-step4-custom-card>
          <div class="qx-step4-card__head">
            <span class="qx-step4-card__icon qx-step4-card__icon--clock" aria-hidden="true"><i class="far fa-calendar-alt"></i></span>
            <h4 class="qx-step4-card__title">{t.step4CustomTitle}</h4>
          </div>
          <div class="qx-step4-custom-intro" data-booking-step4-custom-intro>
            <ul class="qx-step4-custom-list">
              <li><i class="fas fa-check" aria-hidden="true"></i> {t.step4CustomF1}</li>
              <li><i class="fas fa-check" aria-hidden="true"></i> {t.step4CustomF2}</li>
              <li><i class="fas fa-check" aria-hidden="true"></i> {t.step4CustomF3}</li>
            </ul>
            <button type="button" class="qx-step4-custom-cta" data-booking-step4-custom>
              {t.step4CustomCta} <i class="fas fa-arrow-right" aria-hidden="true"></i>
            </button>
          </div>
          <div class="qx-step4-custom-picker-panel" id="booking-step4-custom-picker" role="group" aria-label={t.step4PickDateLabel} hidden>
            <div class="qx-step4-cal-surface">
              <header class="qx-step4-cal-header">
                <span class="qx-step4-picker-lead" id="booking-custom-date-lead">{t.step4PickDateLabel}</span>
                <p class="qx-step4-inline-cal-summary" aria-live="polite">
                  <span data-booking-calendar-trigger-label>{t.step4CalendarTrigger}</span>
                </p>
              </header>
              <input type="hidden" id="booking-custom-date-input" />
              <div class="qx-inline-cal" data-booking-inline-cal aria-labelledby="booking-custom-date-lead">
                <div class="qx-inline-cal__nav">
                  <button type="button" class="qx-inline-cal__nav-btn" data-booking-cal-prev aria-label={t.step4CalPrevMonth}>
                    <i class="fas fa-chevron-left" aria-hidden="true"></i>
                  </button>
                  <span class="qx-inline-cal__monthyear" data-booking-cal-monthyear></span>
                  <button type="button" class="qx-inline-cal__nav-btn" data-booking-cal-next aria-label={t.step4CalNextMonth}>
                    <i class="fas fa-chevron-right" aria-hidden="true"></i>
                  </button>
                </div>
                <div class="qx-inline-cal__weekdays" data-booking-cal-weekdays></div>
                <div class="qx-inline-cal__grid" data-booking-cal-grid role="grid"></div>
              </div>
            </div>
            <p class="qx-step4-date-hint">{t.step4PickDateHint}</p>
            <button type="button" class="qx-step4-custom-back" data-booking-step4-custom-back>
              <i class="fas fa-arrow-left" aria-hidden="true"></i> {t.back}
            </button>
          </div>
        </article>
      </div>
      <p class="qx-step4-error" id="booking-step4-error" hidden role="alert"></p>
    </section>
  </div>

  <div class="form-navigation">
    <button type="button" class="btn-prev">{t.back}</button>
    <button type="button" class="btn-next">{t.continueBtn}</button>
  </div>
</div>

<script define:vars={{ lang, needDate: t.needDate, needCustomDate: t.needCustomDate, needMonday: t.needMonday, step3Subtitle: t.step3Subtitle, step3SubtitleOnline: t.step3SubtitleOnline }}>
  document.addEventListener('DOMContentLoaded', function () {
    const step = document.querySelector('.booking-lesson-time-step');
    if (!step) return;

    const timeSlots = step.querySelector('#booking-time-slots');
    const lessonTimeSubtitle = step.querySelector('#booking-lesson-time-subtitle');
    const tzMorning = step.querySelector('#booking-tz-morning');
    const tzAfternoon = step.querySelector('#booking-tz-afternoon');
    const tzEvening = step.querySelector('#booking-tz-evening');
    const tzSlotHidden = step.querySelector('#booking-timezone-slot');
    const startDateHidden = step.querySelector('#start-date');
    const startDateMode = step.querySelector('#booking-start-date-mode');
    const presetsWrap = step.querySelector('#booking-step4-presets');
    const customCard = step.querySelector('[data-booking-step4-custom-card]');
    const customCta = step.querySelector('[data-booking-step4-custom]');
    const customIntro = step.querySelector('[data-booking-step4-custom-intro]');
    const customPicker = step.querySelector('#booking-step4-custom-picker');
    const customDateInput = step.querySelector('#booking-custom-date-input');
    const errorEl = step.querySelector('#booking-step4-error');
    const calRoot = step.querySelector('[data-booking-inline-cal]');
    const calMonthYearEl = step.querySelector('[data-booking-cal-monthyear]');
    const calWeekdaysEl = step.querySelector('[data-booking-cal-weekdays]');
    const calGridEl = step.querySelector('[data-booking-cal-grid]');
    const calPrevBtn = step.querySelector('[data-booking-cal-prev]');
    const calNextBtn = step.querySelector('[data-booking-cal-next]');
    const calTriggerLabel = step.querySelector('[data-booking-calendar-trigger-label]');

    let calViewYear = new Date().getFullYear();
    let calViewMonth = new Date().getMonth() + 1;

    function startOfToday() {
      const t = new Date();
      t.setHours(0, 0, 0, 0);
      return t;
    }

    /** Build’de kalmış geçmiş / sonraki yıl seçeneklerini gizle */
    function prunePastPresetDates() {
      if (!presetsWrap) return;
      const today = startOfToday();
      const year = today.getFullYear();
      presetsWrap.querySelectorAll('.qx-step4-preset-row').forEach(function (row) {
        const input = row.querySelector('input[type="radio"]');
        if (!(input instanceof HTMLInputElement)) return;
        const d = new Date(input.value + 'T12:00:00');
        if (Number.isNaN(d.getTime()) || d < today || d.getFullYear() !== year) {
          row.remove();
        }
      });
    }
    prunePastPresetDates();

    function localeForLang(lc) {
      if (lc === 'de') return 'de-DE';
      if (lc === 'en') return 'en-GB';
      if (lc === 'es') return 'es-ES';
      return 'tr-TR';
    }

    function pad2(n) { return String(n).padStart(2, '0'); }

    function isoToDisplayValue(iso) {
      const [y, m, d] = iso.split('-');
      if (!d || !m) return '';
      return `${parseInt(d, 10)}.${parseInt(m, 10)}`;
    }

    function hideError() {
      if (!errorEl) return;
      errorEl.hidden = true;
      errorEl.textContent = '';
    }

    function showError(msg) {
      if (!errorEl) return;
      errorEl.textContent = msg;
      errorEl.hidden = false;
    }

    timeSlots?.querySelectorAll('.qx-time-slot').forEach(function (btn) {
      btn.addEventListener('click', function () {
        if (btn.hidden) return;
        timeSlots.querySelectorAll('.qx-time-slot').forEach(function (el) {
          el.classList.remove('is-selected');
          el.setAttribute('aria-pressed', 'false');
        });
        btn.classList.add('is-selected');
        btn.setAttribute('aria-pressed', 'true');
        const val = btn.getAttribute('data-timezone-value') || '';
        if (tzSlotHidden) tzSlotHidden.value = val;
        if (tzMorning) tzMorning.checked = val === 'morning';
        if (tzAfternoon) tzAfternoon.checked = val === 'afternoon';
        if (tzEvening) tzEvening.checked = val === 'evening';
        hideError();
      });
    });

    function clearTimeSlotSelection() {
      timeSlots?.querySelectorAll('.qx-time-slot').forEach(function (el) {
        el.classList.remove('is-selected');
        el.setAttribute('aria-pressed', 'false');
      });
      if (tzSlotHidden) tzSlotHidden.value = '';
      if (tzMorning) tzMorning.checked = false;
      if (tzAfternoon) tzAfternoon.checked = false;
      if (tzEvening) tzEvening.checked = false;
    }

    function selectTimeSlot(value) {
      const btn = timeSlots?.querySelector('.qx-time-slot[data-timezone-value="' + value + '"]');
      if (!btn) return;
      timeSlots?.querySelectorAll('.qx-time-slot').forEach(function (el) {
        el.classList.remove('is-selected');
        el.setAttribute('aria-pressed', 'false');
      });
      btn.classList.add('is-selected');
      btn.setAttribute('aria-pressed', 'true');
      const val = btn.getAttribute('data-timezone-value') || '';
      if (tzSlotHidden) tzSlotHidden.value = val;
      if (tzMorning) tzMorning.checked = val === 'morning';
      if (tzAfternoon) tzAfternoon.checked = val === 'afternoon';
      if (tzEvening) tzEvening.checked = val === 'evening';
      hideError();
    }

    window.syncBookingLessonTimeForProgram = function (program) {
      const isOnline = program === 'online';
      if (timeSlots) timeSlots.classList.toggle('qx-time-slots--online', isOnline);
      if (lessonTimeSubtitle) {
        lessonTimeSubtitle.textContent = isOnline ? step3SubtitleOnline : step3Subtitle;
      }
      clearTimeSlotSelection();
      if (isOnline) {
        selectTimeSlot('evening');
      }
    };

    function syncStep4DateCardStates() {
      const hasPreset = !!presetsWrap?.querySelector('input[type="radio"]:checked');
      const customActive = customCard?.classList.contains('is-picking-custom');
      if (hasPreset) {
        customCard?.classList.add('is-locked');
        customCard?.setAttribute('aria-disabled', 'true');
        if (customCta) {
          customCta.disabled = true;
          customCta.setAttribute('aria-disabled', 'true');
        }
      } else if (!customActive) {
        customCard?.classList.remove('is-locked');
        customCard?.removeAttribute('aria-disabled');
        if (customCta) {
          customCta.disabled = false;
          customCta.removeAttribute('aria-disabled');
        }
      }
    }

    function clearStartDateSelection() {
      if (startDateHidden) startDateHidden.value = '';
      if (startDateMode) startDateMode.value = '';
      presetsWrap?.querySelectorAll('input[type="radio"]').forEach(function (r) { r.checked = false; });
      customCard?.classList.remove('is-selected', 'is-picking-custom', 'is-locked');
      customCard?.removeAttribute('aria-disabled');
      if (customPicker) customPicker.hidden = true;
      if (customIntro) customIntro.hidden = false;
      if (customDateInput) customDateInput.value = '';
      if (customCta) {
        customCta.disabled = false;
        customCta.removeAttribute('aria-disabled');
      }
      updateCalendarTriggerLabel();
      syncStep4DateCardStates();
    }

    presetsWrap?.addEventListener('mousedown', function (ev) {
      const row = ev.target.closest('.qx-step4-preset-row');
      if (!row) return;
      const radio = row.querySelector('input[type="radio"]');
      if (!(radio instanceof HTMLInputElement)) return;
      if (radio.checked) {
        radio.dataset.presetWasChecked = '1';
      } else {
        delete radio.dataset.presetWasChecked;
      }
    });

    presetsWrap?.addEventListener('click', function (ev) {
      const row = ev.target.closest('.qx-step4-preset-row');
      if (!row) return;
      const radio = row.querySelector('input[type="radio"]');
      if (!(radio instanceof HTMLInputElement)) return;
      if (radio.dataset.presetWasChecked === '1') {
        ev.preventDefault();
        ev.stopPropagation();
        radio.checked = false;
        delete radio.dataset.presetWasChecked;
        if (startDateHidden) startDateHidden.value = '';
        if (startDateMode) startDateMode.value = '';
        hideError();
        syncStep4DateCardStates();
      }
    });

    presetsWrap?.addEventListener('change', function (ev) {
      const target = ev.target;
      if (!(target instanceof HTMLInputElement) || target.type !== 'radio' || !target.checked) return;
      customCard?.classList.remove('is-selected', 'is-picking-custom');
      if (customPicker) customPicker.hidden = true;
      if (customIntro) customIntro.hidden = false;
      if (customDateInput) customDateInput.value = '';
      if (startDateMode) startDateMode.value = 'preset';
      if (startDateHidden) startDateHidden.value = target.getAttribute('data-display-value') || isoToDisplayValue(target.value);
      hideError();
      syncStep4DateCardStates();
    });

    customCta?.addEventListener('click', function () {
      if (customCard?.classList.contains('is-locked')) return;
      presetsWrap?.querySelectorAll('input[type="radio"]').forEach(function (r) { r.checked = false; });
      customCard?.classList.add('is-selected', 'is-picking-custom');
      if (customIntro) customIntro.hidden = true;
      if (customPicker) customPicker.hidden = false;
      if (startDateMode) startDateMode.value = 'custom';
      if (startDateHidden) startDateHidden.value = '';
      syncCalViewToMin();
      renderInlineCalendar();
      hideError();
      syncStep4DateCardStates();
    });

    step.querySelector('[data-booking-step4-custom-back]')?.addEventListener('click', function () {
      customCard?.classList.remove('is-selected', 'is-picking-custom');
      if (customPicker) customPicker.hidden = true;
      if (customIntro) customIntro.hidden = false;
      if (customDateInput) customDateInput.value = '';
      if (startDateMode) startDateMode.value = '';
      if (startDateHidden) startDateHidden.value = '';
      hideError();
      syncStep4DateCardStates();
    });

    function calBounds() {
      const min = startOfToday();
      const max = new Date(min.getFullYear(), 11, 31);
      max.setHours(0, 0, 0, 0);
      return { min, max };
    }

    function syncCalViewToMin() {
      const { min } = calBounds();
      calViewYear = min.getFullYear();
      calViewMonth = min.getMonth() + 1;
    }

    function formatFullCustomStartLabel(iso) {
      const d = new Date(`${iso}T12:00:00`);
      return new Intl.DateTimeFormat(localeForLang(lang), { weekday: 'short', day: 'numeric', month: 'long', year: 'numeric' }).format(d);
    }

    function updateCalendarTriggerLabel() {
      if (!calTriggerLabel) return;
      const iso = customDateInput?.value?.trim() || '';
      calTriggerLabel.textContent = iso ? formatFullCustomStartLabel(iso) : calTriggerLabel.getAttribute('data-fallback') || '';
    }

    if (calTriggerLabel) calTriggerLabel.setAttribute('data-fallback', calTriggerLabel.textContent || '');

    function fillCalWeekdayHeaders() {
      if (!calWeekdaysEl) return;
      calWeekdaysEl.innerHTML = '';
      const loc = localeForLang(lang);
      for (let i = 0; i < 7; i++) {
        const dd = new Date(2024, 0, 1 + i);
        const el = document.createElement('div');
        el.className = 'qx-inline-cal__weekday';
        el.textContent = new Intl.DateTimeFormat(loc, { weekday: 'narrow' }).format(dd);
        calWeekdaysEl.appendChild(el);
      }
    }

    function renderInlineCalendar() {
      if (!calGridEl || !calMonthYearEl) return;
      const { min, max } = calBounds();
      const loc = localeForLang(lang);
      calMonthYearEl.textContent = new Intl.DateTimeFormat(loc, { month: 'long', year: 'numeric' }).format(new Date(calViewYear, calViewMonth - 1, 1));
      if (calWeekdaysEl && calWeekdaysEl.childElementCount === 0) fillCalWeekdayHeaders();

      const first = new Date(calViewYear, calViewMonth - 1, 1);
      const startPad = (first.getDay() + 6) % 7;
      const daysInMonth = new Date(calViewYear, calViewMonth, 0).getDate();
      const selectedIso = customDateInput?.value?.trim() || '';

      calGridEl.innerHTML = '';
      let dayNum = 1;
      for (let i = 0; i < 42; i++) {
        const cell = document.createElement('div');
        cell.className = 'qx-inline-cal__cell';
        if (i < startPad || dayNum > daysInMonth) {
          const pad = document.createElement('span');
          pad.className = 'qx-inline-cal__pad';
          pad.setAttribute('aria-hidden', 'true');
          cell.appendChild(pad);
        } else {
          const iso = `${calViewYear}-${pad2(calViewMonth)}-${pad2(dayNum)}`;
          const cellDate = new Date(calViewYear, calViewMonth - 1, dayNum);
          cellDate.setHours(0, 0, 0, 0);
          const btn = document.createElement('button');
          btn.type = 'button';
          btn.className = 'qx-inline-cal__day';
          btn.textContent = String(dayNum);
          btn.dataset.calDay = iso;
          const isMonday = cellDate.getDay() === 1;
          if (cellDate < min || cellDate > max || !isMonday) {
            btn.disabled = true;
            btn.classList.add('is-out');
            if (!isMonday) btn.classList.add('is-not-monday');
          }
          if (iso === selectedIso) btn.classList.add('is-selected');
          cell.appendChild(btn);
          dayNum++;
        }
        calGridEl.appendChild(cell);
      }
    }

    calGridEl?.addEventListener('click', function (ev) {
      const hit = ev.target.closest('button[data-cal-day]');
      if (!hit || hit.disabled) return;
      const iso = hit.dataset.calDay;
      if (!iso || !customDateInput) return;
      customDateInput.value = iso;
      if (startDateHidden) startDateHidden.value = isoToDisplayValue(iso);
      if (startDateMode) startDateMode.value = 'custom';
      updateCalendarTriggerLabel();
      hideError();
      renderInlineCalendar();
    });

    calPrevBtn?.addEventListener('click', function () {
      const { min } = calBounds();
      const nextM = calViewMonth - 1;
      const nextY = nextM < 1 ? calViewYear - 1 : calViewYear;
      const nextMonth = nextM < 1 ? 12 : nextM;
      if (new Date(nextY, nextMonth - 1, 1) < new Date(min.getFullYear(), min.getMonth(), 1)) return;
      calViewYear = nextY;
      calViewMonth = nextMonth;
      renderInlineCalendar();
    });

    calNextBtn?.addEventListener('click', function () {
      const { max } = calBounds();
      const nextM = calViewMonth + 1;
      const nextY = nextM > 12 ? calViewYear + 1 : calViewYear;
      const nextMonth = nextM > 12 ? 1 : nextM;
      if (new Date(nextY, nextMonth - 1, 1) > new Date(max.getFullYear(), max.getMonth(), 1)) return;
      calViewYear = nextY;
      calViewMonth = nextMonth;
      renderInlineCalendar();
    });

    window.validateBookingLessonTimeStep = function () {
      hideError();
      // Ders saati isteğe bağlı — seçilmeden devam edilebilir
      const mode = startDateMode?.value?.trim();
      const dateVal = startDateHidden?.value?.trim();
      if (!mode || !dateVal) {
        showError(needDate);
        return false;
      }
      if (mode === 'custom') {
        const customIso = customDateInput?.value?.trim() || '';
        if (!customIso) {
          showError(needCustomDate);
          return false;
        }
        const d = new Date(`${customIso}T12:00:00`);
        const yearNow = startOfToday().getFullYear();
        if (Number.isNaN(d.getTime()) || d.getDay() !== 1 || d.getFullYear() !== yearNow || d < startOfToday()) {
          showError(needMonday);
          return false;
        }
      }
      return true;
    };

    window.resetBookingLessonTimeStep = clearStartDateSelection;

    window.animateBookingLessonTimeSlots = function () {
      if (!timeSlots) return;
      timeSlots.querySelectorAll('.qx-time-slot').forEach(function (btn) {
        if (btn.offsetParent === null) return;
        btn.classList.remove('qx-time-slot--shake');
        void btn.offsetWidth;
        btn.classList.add('qx-time-slot--shake');
        btn.addEventListener('animationend', function onEnd() {
          btn.classList.remove('qx-time-slot--shake');
          btn.removeEventListener('animationend', onEnd);
        });
      });
    };

    const initialProgram = document.querySelector('[name="program"]:checked')?.value || '';
    if (initialProgram) window.syncBookingLessonTimeForProgram(initialProgram);
  });
</script>

<style>
  .booking-qx-wrap {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }
  .booking-lesson-time-step .qx-question {
    font-size: 1.15rem;
  }
  .booking-step4-trust {
    margin: 0 0 1rem;
  }
  .booking-qx-wrap .qx-time-slots {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 1rem;
  }
  .booking-lesson-time-step .qx-time-slot--evening {
    display: none !important;
  }
  .booking-lesson-time-step .qx-time-slots--online .qx-time-slot--morning,
  .booking-lesson-time-step .qx-time-slots--online .qx-time-slot--afternoon {
    display: none !important;
  }
  .booking-lesson-time-step .qx-time-slots--online .qx-time-slot--evening {
    display: flex !important;
  }
  .booking-lesson-time-step .qx-time-slots--online {
    grid-template-columns: 1fr;
    max-width: 320px;
  }
  .booking-lesson-time-step .qx-step4-card--custom.is-locked {
    opacity: 0.48;
    filter: grayscale(0.4);
    pointer-events: none;
    user-select: none;
    border-color: rgba(226, 232, 240, 0.85);
    box-shadow: none;
    position: relative;
  }
  .booking-lesson-time-step .qx-step4-card--custom.is-locked .qx-step4-card__head,
  .booking-lesson-time-step .qx-step4-card--custom.is-locked .qx-step4-custom-intro {
    opacity: 0.85;
  }
  .booking-lesson-time-step .qx-step4-card--custom.is-locked .qx-step4-custom-cta {
    cursor: not-allowed;
    background: #e2e8f0;
    color: #94a3b8;
    border-color: #e2e8f0;
    box-shadow: none;
  }
  .booking-lesson-time-step .qx-step4-card--presets:has(.qx-step4-preset-radio:checked) {
    border-color: rgba(12, 192, 223, 0.45);
    box-shadow:
      0 0 0 3px rgba(12, 192, 223, 0.12),
      0 10px 28px -8px rgba(12, 192, 223, 0.15);
  }
  .booking-lesson-time-step .qx-time-slot {
    position: relative;
    transition: border-color 0.2s ease, box-shadow 0.2s ease, transform 0.2s ease, background 0.2s ease;
  }
  .booking-lesson-time-step .qx-time-slot.is-selected {
    border-color: rgba(12, 192, 223, 0.95);
    background: linear-gradient(165deg, rgba(12, 192, 223, 0.1) 0%, rgba(255, 210, 31, 0.14) 100%);
    box-shadow:
      0 0 0 3px rgba(12, 192, 223, 0.28),
      0 10px 28px -6px rgba(12, 192, 223, 0.35);
    transform: translateY(-2px);
  }
  .booking-lesson-time-step .qx-time-slot.is-selected::after {
    content: '✓';
    position: absolute;
    top: 0.65rem;
    right: 0.75rem;
    width: 1.65rem;
    height: 1.65rem;
    border-radius: 999px;
    background: #0cc0df;
    color: #fff;
    font-size: 0.85rem;
    font-weight: 800;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 2px 8px rgba(12, 192, 223, 0.45);
  }
  .booking-lesson-time-step .qx-time-slot.is-selected .qx-time-slot__period-label,
  .booking-lesson-time-step .qx-time-slot.is-selected .qx-time-slot__period-clock {
    color: #0a4d6e;
    font-weight: 800;
  }
  @keyframes qx-time-slot-shake {
    0%, 100% { transform: translateX(0); }
    15% { transform: translateX(-7px); }
    30% { transform: translateX(7px); }
    45% { transform: translateX(-5px); }
    60% { transform: translateX(5px); }
    75% { transform: translateX(-2px); }
    90% { transform: translateX(2px); }
  }
  .booking-lesson-time-step .qx-time-slot--shake:not(.is-selected) {
    animation: qx-time-slot-shake 0.65s ease-in-out;
  }
  .booking-lesson-time-step .qx-time-slot--shake.is-selected {
    animation: none;
  }
  @media (max-width: 768px) {
    .booking-lesson-time-step .qx-time-slots:not(.qx-time-slots--online) {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 0.5rem;
    }
    .booking-lesson-time-step .qx-time-slots--online {
      grid-template-columns: 1fr;
      max-width: none;
    }
    .booking-lesson-time-step .qx-time-slot__period {
      padding: 0.5rem 0.55rem 0.55rem;
      gap: 0.45rem;
    }
    .booking-lesson-time-step .qx-time-slot__icon {
      width: 2.1rem;
      height: 2.1rem;
    }
    .booking-lesson-time-step .qx-time-slot__icon .fas {
      font-size: 0.82rem;
    }
    .booking-lesson-time-step .qx-time-slot__period-label {
      font-size: 0.72rem;
      line-height: 1.2;
    }
    .booking-lesson-time-step .qx-time-slot__period-clock {
      font-size: 0.64rem;
    }
    .booking-lesson-time-step .qx-time-slot__main {
      padding: 0.35rem 0.45rem 0.45rem;
    }
    .booking-lesson-time-step .qx-time-slot__meta {
      font-size: 0.6rem;
      line-height: 1.25;
    }
    .booking-lesson-time-step .qx-time-slot.is-selected::after {
      width: 1.35rem;
      height: 1.35rem;
      font-size: 0.72rem;
      top: 0.4rem;
      right: 0.45rem;
    }
    .booking-lesson-time-step .qx-scene-head--step3 .qx-question {
      font-size: 1rem;
    }
    .booking-lesson-time-step .qx-question-note {
      font-size: 0.8rem;
    }
  }
</style>
