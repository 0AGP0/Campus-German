---
import '../styles/pages/quote-wizard.css';
const {
  lang = 'tr',
  bookingPath = '/tr/buchung',
  homePath: homePathProp,
  embedded = false,
  modalMode = false,
  /** Aynı sayfada modal + gömülü için benzersiz id: modal='m', sayfa gövdesi='e' */
  instanceSuffix = 'm',
} = Astro.props;
const homePath = homePathProp ?? `/${lang}`;
const quoteMode = embedded ? 'embedded' : 'standalone';
const pw = (base: string) => `${base}-${instanceSuffix}`;
const BOOKING_FREE_WEBHOOK = 'https://hook.eu2.make.com/40s1h4a3wra21aszpa9y9erfsfooso47';
const quoteWebhook = import.meta.env.PUBLIC_MAKE_QUOTE_WEBHOOK || BOOKING_FREE_WEBHOOK;

type ChipOption = { value: string; label: string };

const copy = {
  tr: {
    title: 'Fiyat teklifi al',
    subtitle: 'Birkaç bilgiyle size özel teklif hazırlayalım.',
    sectionContact: 'İletişim bilgileri',
    sectionPrefs: 'Birkaç soru',
    tabInfo: 'Bilgileriniz',
    tabPrefs: 'Tercihleriniz',
    fullName: 'İsim Soyisim',
    fullNamePlaceholder: 'Adınız ve soyadınız',
    ageRange: 'Yaş aralığınız?',
    occupation: 'Meslek',
    occupationPlaceholder: 'Mesleğiniz',
    country: 'Ülke',
    countryPlaceholder: 'Yaşadığınız ülke',
    startTimeline: 'Ne zaman başlamak istersiniz?',
    hasFriend: 'Birlikte gelecek arkadaşınız var mı?',
    friendName: 'Arkadaşınızın adı',
    friendNamePlaceholder: 'Arkadaşınızın adı',
    germanLevel: 'Almanca seviyeniz?',
    interestedCourse: 'Hangi kurs ilginizi çekiyor?',
    visaSupport: 'Vize desteği ister misiniz?',
    agencyName: 'Acenta',
    agencyNamePlaceholder: 'Acenta adı',
    email: 'E-posta',
    emailPlaceholder: 'ornek@mail.com',
    phone: 'Telefon',
    phonePlaceholder: '5XX XXX XX XX',
    contactHint: 'E-posta veya telefondan biri yeterli.',
    submit: 'Teklifimi Al',
    doneTitle: 'Teşekkürler',
    doneText: 'Danismanlarimiz size ulasacak.',
    doneClose: 'Ana sayfaya dön',
    close: 'Kapat',
    error: 'Form gönderilemedi. Lütfen tekrar deneyin.',
    sending: 'Gönderiliyor...',
    success: 'Formunuz başarıyla gönderildi.',
    validation: {
      contactRequired: 'Lütfen e-posta veya telefon girin.',
      emailInvalid: 'Geçerli bir e-posta adresi girin.',
      required: 'Lütfen tüm zorunlu alanları doldurun.',
    },
    chips: {
      ageRange: [
        { value: '18-24', label: '18–24' },
        { value: '25-34', label: '25–34' },
        { value: '35-44', label: '35–44' },
        { value: '45-54', label: '45–54' },
        { value: '55+', label: '55+' },
      ],
      startTimeline: [
        { value: 'immediate', label: 'Hemen' },
        { value: '1-2-months', label: '1–2 Ay' },
        { value: '3-6-months', label: '3–6 Ay' },
        { value: 'later', label: 'Daha sonra' },
      ],
      hasFriend: [
        { value: 'yes', label: 'Evet' },
        { value: 'no', label: 'Hayır' },
      ],
      germanLevel: [
        { value: 'A0', label: 'A0' },
        { value: 'A1', label: 'A1' },
        { value: 'A2', label: 'A2' },
        { value: 'B1', label: 'B1' },
        { value: 'B2', label: 'B2' },
        { value: 'C1', label: 'C1' },
      ],
      interestedCourse: [
        { value: 'intensive', label: 'Yoğun' },
        { value: 'evening', label: 'Akşam' },
        { value: 'vocational', label: 'Mesleki' },
        { value: 'exam-prep', label: 'Sınav' },
        { value: 'online', label: 'Online' },
      ],
      visaSupport: [
        { value: 'yes', label: 'Evet' },
        { value: 'no', label: 'Hayır' },
      ],
    },
  },
  de: {
    title: 'Preisangebot anfordern',
    subtitle: 'Mit wenigen Angaben erstellen wir Ihr persönliches Angebot.',
    sectionContact: 'Kontaktdaten',
    sectionPrefs: 'Ein paar Fragen',
    tabInfo: 'Ihre Angaben',
    tabPrefs: 'Ihre Präferenzen',
    fullName: 'Vor- und Nachname',
    fullNamePlaceholder: 'Ihr vollständiger Name',
    ageRange: 'Wie alt sind Sie?',
    occupation: 'Beruf',
    occupationPlaceholder: 'Ihr Beruf',
    country: 'Land',
    countryPlaceholder: 'Ihr Wohnsitzland',
    startTimeline: 'Wann möchten Sie starten?',
    hasFriend: 'Lernen Sie mit einer Begleitung?',
    friendName: 'Name der Begleitung',
    friendNamePlaceholder: 'Name eingeben',
    germanLevel: 'Welches Deutsch-Niveau haben Sie?',
    interestedCourse: 'Welcher Kurs interessiert Sie?',
    visaSupport: 'Benötigen Sie Visum-Unterstützung?',
    agencyName: 'Agentur',
    agencyNamePlaceholder: 'Name der Agentur',
    email: 'E-Mail',
    emailPlaceholder: 'name@mail.de',
    phone: 'Telefon',
    phonePlaceholder: '+49 …',
    contactHint: 'E-Mail oder Telefon genügt.',
    submit: 'Angebot anfordern',
    doneTitle: 'Danke',
    doneText: 'Unsere Berater melden sich bei Ihnen.',
    doneClose: 'Zur Startseite',
    close: 'Schliessen',
    error: 'Formular konnte nicht gesendet werden.',
    sending: 'Wird gesendet...',
    success: 'Ihr Formular wurde erfolgreich gesendet.',
    validation: {
      contactRequired: 'Bitte geben Sie E-Mail oder Telefon ein.',
      emailInvalid: 'Bitte geben Sie eine gültige E-Mail-Adresse ein.',
      required: 'Bitte füllen Sie alle Pflichtfelder aus.',
    },
    chips: {
      ageRange: [
        { value: '18-24', label: '18–24' },
        { value: '25-34', label: '25–34' },
        { value: '35-44', label: '35–44' },
        { value: '45-54', label: '45–54' },
        { value: '55+', label: '55+' },
      ],
      startTimeline: [
        { value: 'immediate', label: 'Sofort' },
        { value: '1-2-months', label: '1–2 Monate' },
        { value: '3-6-months', label: '3–6 Monate' },
        { value: 'later', label: 'Später' },
      ],
      hasFriend: [
        { value: 'yes', label: 'Ja' },
        { value: 'no', label: 'Nein' },
      ],
      germanLevel: [
        { value: 'A0', label: 'A0' },
        { value: 'A1', label: 'A1' },
        { value: 'A2', label: 'A2' },
        { value: 'B1', label: 'B1' },
        { value: 'B2', label: 'B2' },
        { value: 'C1', label: 'C1' },
      ],
      interestedCourse: [
        { value: 'intensive', label: 'Intensiv' },
        { value: 'evening', label: 'Abend' },
        { value: 'vocational', label: 'Beruflich' },
        { value: 'exam-prep', label: 'Prüfung' },
        { value: 'online', label: 'Online' },
      ],
      visaSupport: [
        { value: 'yes', label: 'Ja' },
        { value: 'no', label: 'Nein' },
      ],
    },
  },
  en: {
    title: 'Get a price quote',
    subtitle: 'Share a few details and we will prepare a tailored offer.',
    sectionContact: 'Contact details',
    sectionPrefs: 'A few questions',
    tabInfo: 'Your details',
    tabPrefs: 'Your preferences',
    fullName: 'Full name',
    fullNamePlaceholder: 'Your full name',
    ageRange: 'What is your age range?',
    occupation: 'Occupation',
    occupationPlaceholder: 'Your occupation',
    country: 'Country',
    countryPlaceholder: 'Country of residence',
    startTimeline: 'When would you like to start?',
    hasFriend: 'Will you study with a friend?',
    friendName: "Friend's name",
    friendNamePlaceholder: "Friend's name",
    germanLevel: 'What is your German level?',
    interestedCourse: 'Which course interests you?',
    visaSupport: 'Do you need visa support?',
    agencyName: 'Agency',
    agencyNamePlaceholder: 'Agency name',
    email: 'Email',
    emailPlaceholder: 'you@example.com',
    phone: 'Phone',
    phonePlaceholder: '+49 …',
    contactHint: 'Email or phone — one is enough.',
    submit: 'Get my quote',
    doneTitle: 'Thank you',
    doneText: 'Our advisors will contact you.',
    doneClose: 'Back to home',
    close: 'Close',
    error: 'Form submission failed. Please try again.',
    sending: 'Sending...',
    success: 'Your form was submitted successfully.',
    validation: {
      contactRequired: 'Please enter email or phone.',
      emailInvalid: 'Please enter a valid email address.',
      required: 'Please fill in all required fields.',
    },
    chips: {
      ageRange: [
        { value: '18-24', label: '18–24' },
        { value: '25-34', label: '25–34' },
        { value: '35-44', label: '35–44' },
        { value: '45-54', label: '45–54' },
        { value: '55+', label: '55+' },
      ],
      startTimeline: [
        { value: 'immediate', label: 'Immediately' },
        { value: '1-2-months', label: '1–2 months' },
        { value: '3-6-months', label: '3–6 months' },
        { value: 'later', label: 'Later' },
      ],
      hasFriend: [
        { value: 'yes', label: 'Yes' },
        { value: 'no', label: 'No' },
      ],
      germanLevel: [
        { value: 'A0', label: 'A0' },
        { value: 'A1', label: 'A1' },
        { value: 'A2', label: 'A2' },
        { value: 'B1', label: 'B1' },
        { value: 'B2', label: 'B2' },
        { value: 'C1', label: 'C1' },
      ],
      interestedCourse: [
        { value: 'intensive', label: 'Intensive' },
        { value: 'evening', label: 'Evening' },
        { value: 'vocational', label: 'Vocational' },
        { value: 'exam-prep', label: 'Exam' },
        { value: 'online', label: 'Online' },
      ],
      visaSupport: [
        { value: 'yes', label: 'Yes' },
        { value: 'no', label: 'No' },
      ],
    },
  },
  es: {
    title: 'Solicitar presupuesto',
    subtitle: 'Con unos datos prepararemos una oferta personalizada.',
    sectionContact: 'Datos de contacto',
    sectionPrefs: 'Algunas preguntas',
    tabInfo: 'Tus datos',
    tabPrefs: 'Tus preferencias',
    fullName: 'Nombre completo',
    fullNamePlaceholder: 'Tu nombre completo',
    ageRange: '¿Cuál es tu rango de edad?',
    occupation: 'Ocupación',
    occupationPlaceholder: 'Tu ocupación',
    country: 'País',
    countryPlaceholder: 'País de residencia',
    startTimeline: '¿Cuándo quieres empezar?',
    hasFriend: '¿Estudiarás con un amigo/a?',
    friendName: 'Nombre del amigo/a',
    friendNamePlaceholder: 'Nombre del amigo/a',
    germanLevel: '¿Cuál es tu nivel de alemán?',
    interestedCourse: '¿Qué curso te interesa?',
    visaSupport: '¿Necesitas apoyo con el visado?',
    agencyName: 'Agencia',
    agencyNamePlaceholder: 'Nombre de la agencia',
    email: 'Email',
    emailPlaceholder: 'ejemplo@correo.com',
    phone: 'Teléfono',
    phonePlaceholder: '+34 …',
    contactHint: 'Email o teléfono — uno basta.',
    submit: 'Pedir presupuesto',
    doneTitle: 'Gracias',
    doneText: 'Nuestros asesores te contactaran.',
    doneClose: 'Volver al inicio',
    close: 'Cerrar',
    error: 'No se pudo enviar.',
    sending: 'Enviando...',
    success: 'Tu formulario se envió correctamente.',
    validation: {
      contactRequired: 'Introduce email o teléfono.',
      emailInvalid: 'Introduce una dirección de email válida.',
      required: 'Completa todos los campos obligatorios.',
    },
    chips: {
      ageRange: [
        { value: '18-24', label: '18–24' },
        { value: '25-34', label: '25–34' },
        { value: '35-44', label: '35–44' },
        { value: '45-54', label: '45–54' },
        { value: '55+', label: '55+' },
      ],
      startTimeline: [
        { value: 'immediate', label: 'Inmediatamente' },
        { value: '1-2-months', label: '1–2 meses' },
        { value: '3-6-months', label: '3–6 meses' },
        { value: 'later', label: 'Más tarde' },
      ],
      hasFriend: [
        { value: 'yes', label: 'Sí' },
        { value: 'no', label: 'No' },
      ],
      germanLevel: [
        { value: 'A0', label: 'A0' },
        { value: 'A1', label: 'A1' },
        { value: 'A2', label: 'A2' },
        { value: 'B1', label: 'B1' },
        { value: 'B2', label: 'B2' },
        { value: 'C1', label: 'C1' },
      ],
      interestedCourse: [
        { value: 'intensive', label: 'Intensivo' },
        { value: 'evening', label: 'Tarde' },
        { value: 'vocational', label: 'Profesional' },
        { value: 'exam-prep', label: 'Examen' },
        { value: 'online', label: 'Online' },
      ],
      visaSupport: [
        { value: 'yes', label: 'Sí' },
        { value: 'no', label: 'No' },
      ],
    },
  },
};

const t = copy[lang as keyof typeof copy] ?? copy.tr;

const chipGroups: { name: string; label: string; options: ChipOption[] }[] = [
  { name: 'ageRange', label: t.ageRange, options: t.chips.ageRange },
  { name: 'startTimeline', label: t.startTimeline, options: t.chips.startTimeline },
  { name: 'germanLevel', label: t.germanLevel, options: t.chips.germanLevel },
  { name: 'interestedCourse', label: t.interestedCourse, options: t.chips.interestedCourse },
  { name: 'hasFriend', label: t.hasFriend, options: t.chips.hasFriend },
  { name: 'visaSupport', label: t.visaSupport, options: t.chips.visaSupport },
];

const chipHiddenId = (name: string) => {
  const cap = name.charAt(0).toUpperCase() + name.slice(1);
  return pw(`pw${cap}`);
};
---

<div class="quote-wizard-instance" data-quote-instance={instanceSuffix}>
<div class={`qx-page-bg ${quoteMode === 'standalone' ? 'qx-page-bg--standalone' : 'qx-page-bg--embedded'} ${modalMode ? 'qx-page-bg--modal' : ''}`}>
<section class="qx qx-layout-flow" id={pw('pwRoot')} data-qx-mode={quoteMode}>
  <header class="qx-compact-header">
    <div class="qx-compact-header__logo" aria-hidden={modalMode || embedded ? 'true' : 'false'}>
      <img src="/German_logo.webp" alt="" class="qx-compact-logo" width="40" height="40" loading="lazy" decoding="async" />
    </div>
    <div class="qx-compact-header__copy">
      <h2 class="qx-compact-title">{t.title}</h2>
      <p class="qx-compact-subtitle">{t.subtitle}</p>
    </div>
  </header>

  <div class="qx-compact-body">
    <p class="qx-submit-feedback" id={pw('pwSubmitFeedback')} aria-live="polite"></p>

    <form
      id={pw('pwForm')}
      class="qx-form qx-form--compact"
      data-webhook={quoteWebhook}
      data-error={t.error}
      data-modal-mode={modalMode ? 'true' : 'false'}
      data-sending={t.sending}
      data-success={t.success}
      data-val-contact={t.validation.contactRequired}
      data-val-email={t.validation.emailInvalid}
      data-val-required={t.validation.required}
      novalidate
    >
      <input type="hidden" name="formType" value="price-quote" />
      <input type="hidden" name="sourcePage" value={Astro.url.pathname} />
      <input type="hidden" name="lang" value={lang} />

      <div class="qx-block qx-block--contact" id={pw('qxPanelInfo')} data-qx-tab-panel="info">
        <div class="qx-row qx-row--3">
          <label class="qx-field">
            <span class="qx-field-label">{t.fullName}</span>
            <input type="text" name="fullName" required autocomplete="name" class="qx-input" placeholder={t.fullNamePlaceholder} />
          </label>
          <label class="qx-field">
            <span class="qx-field-label">{t.email}</span>
            <input type="email" name="email" autocomplete="email" class="qx-input" placeholder={t.emailPlaceholder} />
          </label>
          <label class="qx-field">
            <span class="qx-field-label">{t.phone}</span>
            <input type="tel" name="phone" autocomplete="tel" class="qx-input" placeholder={t.phonePlaceholder} />
          </label>
        </div>
        <p class="qx-field-error qx-field-error--contact" id={pw('pwContactError')} hidden role="alert"></p>
        <div class="qx-row qx-row--3">
          <label class="qx-field">
            <span class="qx-field-label">{t.occupation}</span>
            <input type="text" name="occupation" required autocomplete="organization-title" class="qx-input" placeholder={t.occupationPlaceholder} />
          </label>
          <label class="qx-field">
            <span class="qx-field-label">{t.country}</span>
            <input type="text" name="country" required autocomplete="country-name" class="qx-input" placeholder={t.countryPlaceholder} />
          </label>
          <label class="qx-field">
            <span class="qx-field-label">{t.agencyName}</span>
            <input type="text" name="agencyName" required class="qx-input" placeholder={t.agencyNamePlaceholder} />
          </label>
        </div>
      </div>

      <div class="qx-block qx-block--prefs" id={pw('qxPanelPrefs')} data-qx-tab-panel="prefs">
        <div class="qx-pref-list">
          {chipGroups.map((group, i) => (
            <div class={`qx-choice qx-choice--n${group.options.length}`} data-qx-chip-group={group.name}>
              <div class="qx-choice__label">
                <span class="qx-choice__num" aria-hidden="true">{String(i + 1).padStart(2, '0')}</span>
                <span class="qx-choice__q">{group.label}</span>
              </div>
              <div class="qx-choice__options" role="radiogroup" aria-label={group.label}>
                {group.options.map((opt) => (
                  <button
                    type="button"
                    class="qx-choice-btn"
                    data-chip-field={group.name}
                    data-chip-value={opt.value}
                    data-chip-label={opt.label}
                    aria-pressed="false"
                  >{opt.label}</button>
                ))}
              </div>
              <input type="hidden" name={group.name} id={chipHiddenId(group.name)} value="" data-required="true" />
              <p class="qx-field-error" hidden role="alert"></p>
              {group.name === 'hasFriend' && (
                <div class="qx-friend qx-field-group--collapsible" id={pw('friendNameGroup')} hidden aria-hidden="true">
                  <label class="qx-field">
                    <span class="qx-field-label">{t.friendName}</span>
                    <input type="text" name="friendName" id={pw('pwFriendName')} class="qx-input" placeholder={t.friendNamePlaceholder} />
                  </label>
                </div>
              )}
            </div>
          ))}
        </div>
      </div>

      <footer class="qx-footer">
        <button type="submit" class="qx-send" id={pw('pwSubmitBtn')}>
          {t.submit}
        </button>
      </footer>
    </form>
  </div>
</section>
</div>

<div class="pw-overlay" id={pw('pwOverlay')} data-home={homePath} aria-hidden="true">
  <div class="pw-dialog pw-dialog--done" data-pw-dialog role="dialog" aria-modal="true" aria-labelledby={pw('pwDoneTitle')}>
    <button type="button" class="pw-dialog-dismiss" data-pw-close aria-label={t.doneClose}><i class="fas fa-times" aria-hidden="true"></i></button>
    <div class="pw-dialog-icon" aria-hidden="true"><i class="fas fa-check"></i></div>
    <h3 id={pw('pwDoneTitle')} class="pw-dialog-title">{t.doneTitle}</h3>
    <p class="pw-dialog-text">{t.doneText}</p>
    <div class="pw-dialog-cta-wrap">
      <button type="button" class="pw-dialog-home" data-pw-close>{t.doneClose}</button>
    </div>
  </div>
</div>
</div>

<script>
  function initQuoteMount(mount: HTMLElement) {
    const S = mount.dataset.quoteInstance || 'm';
    const q = <T extends HTMLElement = HTMLElement>(stem: string) => mount.querySelector<T>(`#${stem}-${S}`);
    const form = mount.querySelector<HTMLFormElement>('form.qx-form');
    if (!form) return;

    const submitFeedback = q('pwSubmitFeedback');
    const contactError = q('pwContactError');
    const friendNameGroup = q('friendNameGroup');
    const friendNameInput = q<HTMLInputElement>('pwFriendName');
    const submitBtn = q<HTMLButtonElement>('pwSubmitBtn');

    const chipFields = ['ageRange', 'startTimeline', 'hasFriend', 'germanLevel', 'interestedCourse', 'visaSupport'];

    function getHiddenInput(field: string): HTMLInputElement | null {
      return form!.querySelector<HTMLInputElement>(`input[name="${field}"]`);
    }

    function getChipLabel(field: string, value: string): string {
      const btn = form!.querySelector<HTMLButtonElement>(`[data-chip-field="${field}"][data-chip-value="${value}"]`);
      return btn?.dataset.chipLabel || value;
    }

    function switchToFirstInvalidTab() {
      /* Tek sayfa — sekme yok; hatalı alana kaydır */
      const invalid = form!.querySelector('.is-invalid, .qx-field-error:not([hidden])');
      if (invalid instanceof HTMLElement) {
        invalid.scrollIntoView({ behavior: 'smooth', block: 'center' });
      }
    }

    function initTabLayout() {
      form!.querySelectorAll<HTMLElement>('[data-qx-tab-panel]').forEach((panel) => {
        panel.removeAttribute('hidden');
        panel.setAttribute('aria-hidden', 'false');
        panel.classList.add('is-active');
      });
    }

    initTabLayout();

    function clearFieldErrors() {
      form!.querySelectorAll('.qx-field-error').forEach((el) => {
        el.textContent = '';
        el.hidden = true;
      });
      form!.querySelectorAll('.qx-choice.is-invalid, .qx-pref-row.is-invalid, .qx-field-group.is-invalid, .qx-field.is-invalid, .qx-friend.is-invalid').forEach((el) => el.classList.remove('is-invalid'));
    }

    function showGroupError(field: string, msg: string) {
      const group = form!.querySelector(`[data-qx-chip-group="${field}"]`);
      if (!group) return;
      group.classList.add('is-invalid');
      const err = group.querySelector('.qx-field-error');
      if (err instanceof HTMLElement) {
        err.textContent = msg;
        err.hidden = false;
      }
    }

    function markTextFieldInvalid(name: string) {
      const input = form!.querySelector<HTMLInputElement>(`input[name="${name}"]`);
      input?.closest('.qx-field')?.classList.add('is-invalid');
    }

    function toggleFriendName(show: boolean) {
      if (!friendNameGroup || !friendNameInput) return;
      if (show) {
        friendNameGroup.removeAttribute('hidden');
        friendNameGroup.setAttribute('aria-hidden', 'false');
        friendNameGroup.classList.add('is-expanded');
        friendNameInput.required = true;
      } else {
        friendNameGroup.setAttribute('hidden', '');
        friendNameGroup.setAttribute('aria-hidden', 'true');
        friendNameGroup.classList.remove('is-expanded', 'is-invalid');
        friendNameInput.required = false;
        friendNameInput.value = '';
      }
    }

    form.querySelectorAll<HTMLButtonElement>('[data-chip-field]').forEach((btn) => {
      btn.addEventListener('click', () => {
        const field = btn.dataset.chipField || '';
        const value = btn.dataset.chipValue || '';
        const hidden = getHiddenInput(field);
        if (!hidden) return;

        form!.querySelectorAll(`[data-chip-field="${field}"]`).forEach((b) => {
          b.classList.remove('is-active');
          b.setAttribute('aria-pressed', 'false');
        });
        btn.classList.add('is-active');
        btn.setAttribute('aria-pressed', 'true');
        hidden.value = value;

        const group = btn.closest('[data-qx-chip-group]');
        group?.classList.remove('is-invalid');
        const err = group?.querySelector('.qx-field-error');
        if (err instanceof HTMLElement) {
          err.hidden = true;
          err.textContent = '';
        }

        if (field === 'hasFriend') {
          toggleFriendName(value === 'yes');
        }
      });
    });

    function resetForm() {
      form!.reset();
      chipFields.forEach((field) => {
        const hidden = getHiddenInput(field);
        if (hidden) hidden.value = '';
        form!.querySelectorAll(`[data-chip-field="${field}"]`).forEach((b) => {
          b.classList.remove('is-active');
          b.setAttribute('aria-pressed', 'false');
        });
      });
      toggleFriendName(false);
      clearFieldErrors();
      if (submitFeedback) {
        submitFeedback.textContent = '';
        submitFeedback.classList.remove('is-success', 'is-error');
      }
    }

    if (form.dataset.modalMode === 'true') {
      window.addEventListener('campus-quote-modal-close', () => {
        resetForm();
      });
    }

    const overlay = q('pwOverlay');
    const dialog = mount.querySelector<HTMLElement>('[data-pw-dialog]');

    function openOverlay() {
      if (!overlay || !dialog) return;
      overlay.classList.add('is-open');
      overlay.setAttribute('aria-hidden', 'false');
    }

    function dismissToHome() {
      if (form!.dataset.modalMode === 'true') {
        window.closeCampusQuoteModal?.();
        return;
      }
      let target = (overlay?.dataset.home || '/').trim();
      if (!target.startsWith('/')) target = `/${target}`;
      if (target.length > 1 && target.endsWith('/')) target = target.slice(0, -1);
      window.location.href = target;
    }

    mount.querySelectorAll<HTMLButtonElement>('[data-pw-close]').forEach((btn) =>
      btn.addEventListener('click', dismissToHome),
    );
    overlay?.addEventListener('click', (e) => {
      if (e.target === overlay) dismissToHome();
    });

    const DEFAULT_MAKE_WEBHOOK = 'https://hook.eu2.make.com/40s1h4a3wra21aszpa9y9erfsfooso47';

    function validateForm(): boolean {
      clearFieldErrors();
      const valRequired = form!.dataset.valRequired || 'Please fill in all required fields.';
      const valContact = form!.dataset.valContact || 'Please enter email or phone.';
      const valEmail = form!.dataset.valEmail || 'Please enter a valid email address.';
      let valid = true;

      const fullName = (form!.querySelector<HTMLInputElement>('input[name="fullName"]')?.value || '').trim();
      const occupation = (form!.querySelector<HTMLInputElement>('input[name="occupation"]')?.value || '').trim();
      const country = (form!.querySelector<HTMLInputElement>('input[name="country"]')?.value || '').trim();
      const agencyName = (form!.querySelector<HTMLInputElement>('input[name="agencyName"]')?.value || '').trim();
      const email = (form!.querySelector<HTMLInputElement>('input[name="email"]')?.value || '').trim();
      const phone = (form!.querySelector<HTMLInputElement>('input[name="phone"]')?.value || '').trim();
      const hasFriend = getHiddenInput('hasFriend')?.value || '';
      const friendName = (friendNameInput?.value || '').trim();

      if (!fullName) { markTextFieldInvalid('fullName'); valid = false; }
      if (!occupation) { markTextFieldInvalid('occupation'); valid = false; }
      if (!country) { markTextFieldInvalid('country'); valid = false; }
      if (!agencyName) { markTextFieldInvalid('agencyName'); valid = false; }

      chipFields.forEach((field) => {
        const hidden = getHiddenInput(field);
        if (!hidden?.value.trim()) {
          showGroupError(field, valRequired);
          valid = false;
        }
      });

      if (hasFriend === 'yes' && !friendName) {
        valid = false;
        friendNameGroup?.classList.add('is-invalid');
      }

      if (!email && !phone) {
        valid = false;
        if (contactError) {
          contactError.textContent = valContact;
          contactError.hidden = false;
        }
      } else if (email && !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) {
        valid = false;
        if (contactError) {
          contactError.textContent = valEmail;
          contactError.hidden = false;
        }
      }

      if (!valid) switchToFirstInvalidTab();
      return valid;
    }

    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      if (!validateForm()) {
        const msg = form!.dataset.valRequired || form!.dataset.error || 'Error';
        if ((window as unknown as { showSuccessModal?: (t: string, m: string) => void }).showSuccessModal) {
          (window as unknown as { showSuccessModal: (t: string, m: string) => void }).showSuccessModal('error', msg);
        }
        return;
      }

      const originalBtnText = submitBtn?.textContent || '';
      if (submitFeedback) {
        submitFeedback.textContent = '';
        submitFeedback.classList.remove('is-success', 'is-error');
      }
      if (submitBtn) {
        submitBtn.disabled = true;
        submitBtn.textContent = form!.dataset.sending || 'Sending...';
      }

      const fd = new FormData(form!);
      const data = Object.fromEntries(fd.entries()) as Record<string, string>;

      const ageRange = String(data.ageRange || '').trim();
      const startTimeline = String(data.startTimeline || '').trim();
      const germanLevel = String(data.germanLevel || '').trim();
      const interestedCourse = String(data.interestedCourse || '').trim();
      const visaSupport = String(data.visaSupport || '').trim();
      const hasFriendVal = String(data.hasFriend || '').trim();

      const payload = {
        formType: 'price-quote',
        formVersion: 'compact-v1',
        lang: String(data.lang || '').trim(),
        sourcePage: String(data.sourcePage || '').trim() || window.location.pathname,
        submittedAt: new Date().toISOString(),
        customer: {
          fullName: String(data.fullName || '').trim(),
          email: String(data.email || '').trim(),
          phone: String(data.phone || '').trim(),
          ageRange,
          ageRangeLabel: getChipLabel('ageRange', ageRange),
          occupation: String(data.occupation || '').trim(),
          country: String(data.country || '').trim(),
          startTimeline,
          startTimelineLabel: getChipLabel('startTimeline', startTimeline),
          hasFriend: hasFriendVal,
          friendName: hasFriendVal === 'yes' ? String(data.friendName || '').trim() : '',
          germanLevel,
          germanLevelLabel: getChipLabel('germanLevel', germanLevel),
          interestedCourse,
          interestedCourseLabel: getChipLabel('interestedCourse', interestedCourse),
          visaSupportNeeded: visaSupport,
          agencyName: String(data.agencyName || '').trim(),
        },
      };

      try {
        const wh = (form!.dataset.webhook || '').trim() || DEFAULT_MAKE_WEBHOOK;
        const res = await fetch(wh, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(payload),
        });
        if (!res.ok) throw new Error('webhook');
        if (submitFeedback) {
          submitFeedback.textContent = form!.dataset.success || 'Success';
          submitFeedback.classList.add('is-success');
        }
        resetForm();
        openOverlay();
      } catch {
        const msg = form!.dataset.error || 'Error';
        if (submitFeedback) {
          submitFeedback.textContent = msg;
          submitFeedback.classList.add('is-error');
        }
        if ((window as unknown as { showSuccessModal?: (t: string, m: string) => void }).showSuccessModal) {
          (window as unknown as { showSuccessModal: (t: string, m: string) => void }).showSuccessModal('error', msg);
        } else {
          alert(msg);
        }
      } finally {
        if (submitBtn) {
          submitBtn.disabled = false;
          submitBtn.textContent = originalBtnText;
        }
      }
    });
  }

  function initAllQuoteWizards() {
    document.querySelectorAll<HTMLElement>('.quote-wizard-instance').forEach((mount) => {
      if (mount.dataset.pwInit === '1') return;
      mount.dataset.pwInit = '1';
      initQuoteMount(mount);
    });
  }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', initAllQuoteWizards);
  else initAllQuoteWizards();
</script>
